---
import LessonLayout from '../layouts/LessonLayout.astro'
const base = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`
---
<LessonLayout title="LLMs From Scratch" description="用数学推导、SVG 图解与 PyTorch 验证理解大语言模型。">
  <section class="lesson-index"><h2>学习路线</h2><a href={`${base}lessons/mathematics`}><strong>00 · 数学基础</strong><span>从向量、范数、点积到旋转</span></a><a href={`${base}lessons/position-encoding`}><strong>01 · 位置编码</strong><span>sin/cos、相对位置与 RoPE</span></a><a href={`${base}lessons/kv-cache`}><strong>02 · KV cache</strong><span>从整段注意力到逐 token 解码</span></a><a href={`${base}lessons/ai-infra-01`}><strong>AI Infra · 01–04</strong><span>请求、GPU kernel、推理引擎与分布式生产</span></a></section>
</LessonLayout>
